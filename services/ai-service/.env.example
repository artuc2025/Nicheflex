# AI Backend: "gemini" or "mimo"
AI_BACKEND=gemini

# Gemini (if AI_BACKEND=gemini)
GEMINI_API_KEY=your-gemini-api-key
GEMINI_MODEL=gemini-2.0-flash

# MiMo (if AI_BACKEND=mimo) — requires mimo CLI installed
# No additional config needed
